;; This mode requires snippets.el and find-recursive.el
;; Furthermore there needs to be a serious windows integration.
;; For now I will just add this to linux.

(setq load-path (cons "~/.emacs.d/lib/emacs-rails" load-path))
(require 'rails)